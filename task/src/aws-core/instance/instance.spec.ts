describe("Instance", () => {
  it("should launch new instance when duplicate instance not provided", () => {});
  it("should not launch new instance when duplicate instance is there", () => {});
  it("should call delete instance api", () => {});
  it("should wait till instance is live", () => {});
  it("should wait till instance is live and call hasDup function 5 times", () => {});
  it("should wait call hasDup 5 times and  throw error instance not found", () => {});
  it("should run ssh cmd ", () => {});
  it("should verifyssh cmd by verify file ready on server ", () => {});
  it("should should copy files on server", () => {});
});

export const a = "test";
