export interface IGitConfig {
  repoUrl: string;
  projectName: string;
  buildReason: string;
  targetBranch: string;
  clonePath: string;
  buildDirectory: string;
  sourceBranch: string;
  prId: string;
}
